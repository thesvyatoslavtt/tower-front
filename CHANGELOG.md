# Changelog

Append-only. Каждый PR обязан добавить запись.

## [Unreleased]

### Added
- Bootstrap: Vite 6 + React 19 + TS strict + Tailwind 4.
- ESLint 9 flat config с `eslint-plugin-boundaries` (импорт-границы app→widgets→modules→shared).
- Prettier + lint-staged + Husky.
- Redux Toolkit store + RTK Query `baseApi`.
- React Router 7 data router с `AppLayout` (Header + Sidebar + `<Outlet/>`).
- ThemeProvider (light/dark через CSS variables).
- AI-доки: `CLAUDE.md`, `ARCHITECTURE.md`, `CONTRIBUTING.md`, `.memory/*`, `.claude/skills/*` (каркас).
